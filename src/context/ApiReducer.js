import { GET_DOLAR } from '../actions';

export default (state, action) => {
    switch (action.type) {
        case GET_DOLAR:
            return {

            }
    
        default:
            return state;
    }
}